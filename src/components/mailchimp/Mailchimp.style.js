import newBg from "../../assets/riph/riph-bg-again.png";
import styled from "styled-components";

const mailchimpStyleWrapper = styled.section`
    min-height: 914px; 
    padding-top: 133px;
    padding-bottom: 0px;
    background: url(${newBg});
    background-position: center center;
    background-repeat: no-repeat;
    background-size: cover;
    position: relative; 
`

export default mailchimpStyleWrapper;